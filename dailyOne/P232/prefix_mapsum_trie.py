"""
Using prefix tree (trie) edition
  - k is the length of the prefix.
"""


class PrefixMapSum:
    def __init__(self):
        self.map = {}

    def insert(self, key, value):
        """
        associate each letter with a dictionary
        O(k)
        """
        return

    def sum(self, prefix):
        """
        O(k)
        """
        return


# test
from collections import defaultdict

some = defaultdict(dict)
some[0] = 'a'
some[1] = 'b'
print(some)
